import {THREE} from '../three-defs.js';


export const ocean_constants = (() => {
  	return {
		QT_OCEAN_MIN_LOD_RADIUS: 12,
		QT_OCEAN_MIN_NUM_LAYERS: 6,	
		QT_OCEAN_MIN_CELL_RESOLUTION: 4,	//even numbers only
		OCEAN_SIZE: 2000,
  	}
	
})();
